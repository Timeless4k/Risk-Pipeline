cd /mnt/c/Users/guru1/Documents/Github/Risk\ Pipeline

cd /mnt/c/Users/guru1/Documents/Github/Risk-Pipeline


source venv/bin/activate

python3 -m venv torch-nightly
source torch-nightly/bin/activate


# Install PyTorch (CUDA 12.1 wheels) and verify GPU
pip install --upgrade pip setuptools wheel
pip install --extra-index-url https://download.pytorch.org/whl/cu121 torch torchvision torchaudio
python -c "import torch; print('torch', torch.__version__); print('cuda_is_available', torch.cuda.is_available());
import sys; print('device', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'cpu')"

rm -rf data_cache/

python run_pipeline.py --quick
python run_pipeline.py --full
